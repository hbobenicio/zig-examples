pub const Passport = struct {
    // TODO add fields
    // TODO decide if its better to type everything as []const u8 or better structured types (would it ease part 2 solving?)

    pub fn valid(self: *@This()) bool {
        // TODO implement this
        return false;
    }
};
